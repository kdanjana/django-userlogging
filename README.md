# django-userlogging
a simple webapp that signsup new user, login and logout existing users.
user signup is success only when user's username and email already doesnt exist in the db.
The code is run on ubuntu20.04(LTS).
